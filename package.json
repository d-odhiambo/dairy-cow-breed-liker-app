{
  "name": "dairy-cow-breed-api",
  "version": "1.0.0",
  "description": "Dairy Cow Breed API using json-server",
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
