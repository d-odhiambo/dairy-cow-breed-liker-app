{
  "name": "dairy-cow-breed-liker",
  "version": "1.0.0",
  "description": "Dairy Cow Breed Liker App using json-server",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
